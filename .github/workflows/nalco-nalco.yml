name: Nalco PDF Downloader

on:
  workflow_dispatch: {}
  schedule:
    # 09:00, 12:00, 15:00, 18:00 IST = 03:30, 06:30, 09:30, 12:30 UTC
    - cron: "30 3,6,9,12 * * *"

permissions:
  contents: write

concurrency:
  group: nalco-pdf-downloader
  cancel-in-progress: true

env:
  # Not strictly needed by the script, but handy for future; we run inside this folder.
  BASE_DIR: .

jobs:
  scrape:
    runs-on: ubuntu-latest

    # All shell steps below run inside this subfolder
    defaults:
      run:
        working-directory: nalco_circular_downloader

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            pip install requests beautifulsoup4 lxml pdfplumber pandas openpyxl
          fi

      - name: Ensure output directories exist
        run: |
          mkdir -p pdfs
          mkdir -p data

      - name: Run scraper
        env:
          BASE_DIR: ${{ env.BASE_DIR }}
        run: |
          echo "Running nalco_scraper.py in $(pwd)"
          python nalco_scraper.py
          echo "Scraper finished."

      - name: List outputs
        run: |
          echo "PWD: $(pwd)"
          echo "--- pdfs/"
          ls -la pdfs || true
          echo "--- data/"
          ls -la data || true

      # Upload artifacts only if they exist (prevents failures when nothing is produced)
      - name: Upload PDF artifacts (if any)
        if: ${{ hashFiles('nalco_circular_downloader/pdfs/*.pdf') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: nalco-pdfs
          path: nalco_circular_downloader/pdfs/*.pdf
          if-no-files-found: ignore

      - name: Upload Excel artifact (if exists)
        if: ${{ hashFiles('nalco_circular_downloader/data/nalco_prices.xlsx') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: nalco-prices-xlsx
          path: nalco_circular_downloader/data/nalco_prices.xlsx
          if-no-files-found: ignore

      # Commit & push changes (Excel/log/PDFs) if repo content changed
      - name: Commit & push if changed (repo root)
        working-directory: .
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "Update Nalco data ($(date -u +'%Y-%m-%d %H:%M:%S UTC'))"
            git push
          else
            echo "No changes to commit."
          fi
